#include<stdio.h>
#include<math.h>
int main()
{
    int num,x;
    printf("Enter the number :");
    scanf("%d",&num);
    x=pow(num,3);
    printf("The cube of the number is :%d",x);
    return 0;
}

